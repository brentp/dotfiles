where i keep all my .bash*, .vim* files.
to set up vim::

    mkdir -p ~/.vim/ \
       && cd ~/.vim/ \
       && sh setup.sh \
       && echo "so ~/.vim/vimrc" > ~/.vimrc \


git::

    cp gitconfig ~/.gitconfig

